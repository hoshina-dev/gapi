scalar Map

type AdminArea {
  id: ID!
  name: String!
  isoCode: String!
  geometry: Map!
  adminLevel: Int!
  parentID: ID
}

type Query {
  adminAreas(adminLevel: Int): [AdminArea!]!
  adminArea(id: ID!): AdminArea
  adminAreaByCode(code: String!, adminLevel: Int!): AdminArea
}
